[project]
name = "ai-secure-local-prototype"
version = "0.3.0"
description = "Evidence-linked local security triage with a signed, separately operated response gateway."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
