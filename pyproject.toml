[project]
name = "ai-secure-local-prototype"
version = "0.2.0"
description = "Evidence-linked, local-first security triage prototype. Not a production security control."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
