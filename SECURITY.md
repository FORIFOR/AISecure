# Security / 脅威モデルと本番移行条件

## 保護の対象と信頼境界

この版は、単一管理者が自分の端末上でルールと操作体験を検証するためのアプリです。ブラウザ、Pythonプロセス、ローカルDBと鍵の保管場所を同じ管理者が所有する前提です。OS管理者権限を持つ攻撃者、ブラウザ拡張機能、ホストマルウェアへの防御は提供しません。

同じネットワークの第三者、悪意ある別サイト、壊れた手動JSON、偽造されたLLM出力によって、意図しない対応処理が実行されないことを重視します。ブラウザUIはシミュレーション専用です。CLIには、2名の明示承認、検証済みのOktaユーザーID、対応するSystem Logイベントの確認を要求する、対象を限定したセッション失効経路があります。

## 現状の対策

- HTTP待ち受けを `127.0.0.1` に固定。Host・Origin・cross-siteヘッダーを検証。CORSなし。
- 起動ごとのランダムなBearerトークン。ブラウザの永続ストレージに保存せず、URLクエリでの認証を受け付けない。HTMLには秘密やデータを埋め込まず、APIだけを認証付きで提供。
- 静的配信はHTML/CSS/JSの固定4パスのみ。ディレクトリ一覧・任意ファイルアクセスなし。CSP、frame拒否、no-store、nosniff、no-referrer。
- POSTは型・サイズ・Content-Typeとレートを検証。JSONの余分なキー・重複キーを拒否。イベントIDの衝突を検証。
- 生のユーザー・セッション・ファイル識別子は永続化前に仮名化。ファイル本文はスキーマ外。OS上のファイル権限を制限。Windows ACLの詳細は未検証。
- LLMの説明と検知・対応を分離。モデルには実行ツールを渡さない。補助説明に原文ログを渡さず、既知の参照IDと出力スキーマを検証。内容の真偽は人による確認が必要。
- Ollama通信は固定のループバックIPだけ。プロキシとリダイレクトを無効にする。Ollamaプロセスの外向き通信は別途制御が必要。
- 対応計画は5分以内の明示承認が必要。データ更新・期限切れ・再承認・計画内容の変更を拒否。現在のルールから計画を再生成して保存内容と照合。
- スナップショットは内容ハッシュとHMAC、監査記録は順序・リンク・HMACで検証。不整合時は変更を拒否。
- ログ取り込み（`aisecure import`）は読み取り専用。マッピングプロファイルは許可リストにある項目しか指定できず、他の列はスナップショットにもDBにもLLMにも渡らない。シンボリックリンク・通常ファイル以外・サイズ/行数/フィールド長の上限超過を拒否。ソースのSHA-256を出所として記録する。
- 検知閾値は設定として外部化し、値を丸めずに検証。設定のハッシュを起動時に監査チェーンへ記録し、各検知結果にも付与する。感度を下げた変更が記録に残る。
- Oktaの直接経路は実行時に環境変数からトークンを受け取り、HTTPS・同一組織URL・許可したAPIパスに限定する。セッション失効は対象ユーザーIDを検証し、System Logの対応イベントを確認して初めて成功と記録する。トークンはDB・監査ログ・ブラウザへ保存しない。
- `--encrypted` を指定した保管モードでは、外部Secret Managerから渡した32バイト鍵でスナップショット本体と監査ペイロードをAES-GCM暗号化する。鍵はファイルへ保存せず、暗号化ストアと平文ストアの混在・鍵の取り違えを拒否する。暗号化オプションは `cryptography` を使用する。
- `publish-checkpoint` は監査チェーンの件数・先頭からの連鎖先端だけを、別のHTTPS保管先へ専用鍵で署名送信し、保管先が同じ値を返した場合だけ受理と記録する。WORM性や保管先の可用性そのものは外部運用の責任とする。
- 実操作の前に任意の緊急停止ファイルを確認し、ファイルが存在する、または確認できない場合はOkta/APIレスポンダーへの要求を送らない。
- `--require-attested-approvals` を使う場合、外部SSO/RBACゲートウェイが発行した2件のEd25519署名付き承認証明を、計画・スナップショット・操作・役割・短い有効期限へ束縛して検証する。ローカルの承認者ラベルだけでは本人性を証明しない。

実際に攻撃的な入力を与えて確認した結果は `docs/SECURITY_REVIEW.md` に、実運用検証の項目別チェックリスト（済み/未済と検証者）は `docs/SECURITY_CHECKLIST.md` にあります。実装者自身による検証であり、第三者レビューではありません。

## できないこと

1. **標準モードは保管時暗号化ではありません。** SQLiteは平文で、HMACは暗号化でも匿名化でもありません。`--encrypted` と外部鍵を使う本番オプションではスナップショット本体・監査ペイロードをAES-GCMで暗号化しますが、入力ファイル・書き出しJSON・DBのメタデータは機微情報として扱います。ディスク暗号化・鍵管理・アクセス分離・保持期限は本番側で必要です。
2. **監査ログは改ざん不可能ではありません。** 鍵を知らない変更は検出できますが、鍵とDBを同時に取得した攻撃者は偽造できます。末尾削除には `publish-checkpoint` で別保管したチェックポイントが必要です。WORM、透明性ログ、KMSへの接続と保管先の運用保証はありません。
3. **ログの真正性を証明しません。** 攻撃者や誤設定によって入力が欠損・偽造されると検知は変わります。収集元の署名、連番、心拍、時刻同期、mTLS、再送は本番設計が必要です。取り込み時に記録するSHA-256は「読んだファイルが後から変わっていないこと」を示すだけで、そのファイルが正しいログであることの証明ではありません。取り込み処理中は仮名化前の原文（ユーザー名・ファイルパス）がメモリ上に存在します。実行環境の権限管理が別途必要です。
4. **一般的な侵入・漏えいを防止する製品ではありません。** VPN脆弱性を塞ぐコード、通信遮断、ファイル削除、端末隔離の経路は存在しません。一方、限定したOktaユーザーのセッション失効は、明示承認とSystem Logによる成否確認の後に実行できます。「導入すればGSSのような事故を防げた」と主張してはいけません。
5. **誤検知率は合成データでしか測っていません。** `aisecure evaluate` の数値は同梱の合成トラフィックに対するものです。導入先の正常業務での誤検知率ではありません。閾値を静かにすれば見逃しが増えるという関係は `docs/TUNING.md` に示していますが、どこで線を引くかは運用体制の判断です。
6. **現在の安全性を測っていません。** 手動スナップショットであり、最後に見たログの後の変化は分かりません。危険度のP1/P2/P3は優先度で、攻撃成功率や被害確率ではありません。
7. **一般公開するHTTPサーバーではありません。** Python標準HTTPサーバーによるローカル検証版。高負荷、公開環境の認証・セッション・レート制御・TLS・リバースプロキシ信頼境界は未評価です。
8. **四眼原則・SSO・RBACはありません。** Bearerトークンを知る単一の管理者が計画も承認も行えます。「人の承認」と「独立した別担当者の承認」は異なります。
9. **法令遵守・認証取得を保証しません。** 個人情報の法的取扱い、保存地域、委託契約、業種別基準、監査・通報対応などは、実運用の対象と構成を確定して個別確認が必要です。

## 本番化のゲート

- まず読取専用モードで実ログに接続。ログ取得権限を必要最小限とし、全体管理者資格情報を分析サーバーに置かない。
- 制御面（検知・説明）と実行面（機器API・失効権限）を別プロセス・別資格情報・必要なら別ネットワークに分離。
- ポリシーは署名・バージョン管理。変更をコードレビューし、本番承認を行う。LLMがポリシーを直接編集する構成にしない。
- 現在のOkta実行器は対象ユーザーIDの許可形式、承認時の対象IDバインド、事前確認、2名の明示承認、緊急停止、冪等性に近い再確認、System Logによる実行後確認を備える。外部SSO/RBAC発行の署名付き承認を必須にする境界も用意したが、認証ゲートウェイ、短期権限、他プロバイダーの実行経路は導入先で検証が必要で、操作に失敗しても成功と記録しない。
- セッション失効や認証情報ローテーションは単純な「取り消し」ができない。操作別の業務復旧手順を検証する。
- 実行ごとに最新ログ・現在状態を再確認し、古いスナップショットからの破壊的操作を防ぐ。
- 同意・目的・保持期限・閲覧権限・監査・アクセスログを含むデータ処理設計を確定。
- 実際の収集器・実機API・環境別障害時の挙動をE2Eテスト。ログ欠損時の表示、遅延・重複・時間ずれを検証。
- **事案が起きていない実期間のログで誤検知を測り、1日あたりの確認件数が運用可能であることを確認する**（手順は `docs/TUNING.md`）。合成データの結果を実環境の根拠に使わない。
- 独立したセキュリティレビューと侵入テストを実施。現行の自動テストと実装者自身の検証だけを出荷基準にしない。

脆弱性を公開リポジトリで報告する場合、実データ・トークン・秘密鍵を添付しないでください。この成果物では連絡先やGitHub Security Advisory窓口を作成していません。公開前に専用窓口を設定してください。
