# 変更履歴

## v0.2.2 — 2026-09-12

### 変更

- **誤検知ベースラインに正常業務パターンを追加。** 検索インデックスの夜間走査、週次ウイルススキャン、eDiscovery（リーガルホールド）、バッチETLを合成トラフィックに加えた。正常側のノイズを 52,832 → 85,509 イベントに増やした結果、単体の大量参照ルール AS-003 の誤検知は 35/36 → **102/103** に増え、相関ルール AS-004 は **依然として誤検知0**。ノイズを増やすほど「量だけのルール」は悪化し「経路の相関」は保たれるという製品の主張が、より強く裏付けられた。
- UIを完全バイリンガル化（検知内容・計画・パラメータ説明も英語化）。

## v0.2.1 — 2026-09-12

セキュリティ検証（`docs/SECURITY_REVIEW.md`）で見つかった問題の修正です。実装者による攻撃入力の実測に加え、差分を独立レビューに掛けて再検査しました。社外の監査ではありません。

### 修正（検知回避につながるもの）

- **機器名を選ぶだけで他人の資産に成りすませた問題（重要度: 高）。** 識別子の正規化結果 `名前-ハッシュ` がそれ自体有効な識別子だったため、攻撃者が他人の機器の変換後の名前を計算して自分の機器に付けると同一IDになり、観測時刻が新しい方で丸ごと上書きされました。本物のVPN機器の「外部公開」「未適用」が消え、**最優先の相関検知 AS-004 が発火しなくなります。** 生成形式を `名前:ハッシュ` にし、`:` を生の名前として保持しない文字にすることで名前空間を分離しました。ハッシュ長も32→64ビットに延長し、1回の取り込み内での衝突は統合せずエラーにします。
- **仕込んだログ1行で本物の証跡を消せた問題（重要度: 中）。** 同じイベントIDで内容が異なる行を「先着優先で読み飛ばす」ようにしたため、先に置かれた無害な行が後から来た本物の特権ログインを追い出していました。どちらも捨てず、後続に別IDを付与して両方残すようにしました。
- **取り込み元の申告を検証済みとして表示・記録していた問題（重要度: 中）。** `provenance` は手書きJSONにも書けるのに、画面では取り込み元として、監査チェーンには改ざん検知付きの記録として扱われていました。実際にファイルを読んだ実行だけが `verified: true` を主張できるようにし、画面は `[この実行で読み取り]` と `[申告値 / 未検証]` を区別します。
- **深くネストしたJSON 1行で取り込み全体が落ちる問題（重要度: 低）。** Python 3.11 では `RecursionError` になり未処理例外で失敗していました（3.14 では `ValueError` として読み飛ばせていたため手元では再現せず、CIのバージョンマトリクスが検出）。両方を捕捉します。
- **`bytes` に `inf` が入った1行で取り込み全体が落ちる問題（重要度: 低）。** `OverflowError` を捕捉しておらず未処理例外になっていました。非有限数はその行の値のみ不明として扱います。

### 修正（その他）

- **ログ1行で取り込み全体が失敗する問題（検知妨害につながる）。** ユーザー名・セッションID・ファイルパスに制御文字を含む行、および同じイベントIDで内容が異なる行が、取り込み境界を素通りして後段の検証で全体を失敗させていました。ログに1レコード書き込める攻撃者が分析全体を止められる状態でした。境界で行単位に読み飛ばし、理由別に計上し、ID衝突は警告に出すようにしました。
- **`/api/state` がスナップショット全件を返していた問題。** 10万イベントで27.3 MiBでした。要約（資産台帳・取り込み元・種別ごとの件数）のみを返すようにし、イベント全件は `/api/export` 限定にしました（20万件の上限つき）。同エンドポイントで状態を二重計算していたのも解消しています。
- `baseline` が未来日付のシナリオを生成し `analyze` に渡せなかった問題。`--start` を追加し、未来になる場合は警告するようにしました。
- `import --out` が仮名化前の識別子を含むファイルを出すことを、実行時に警告するようにしました（従来から文書には記載）。
- 画面が根拠IDを全件描画していたのを200件までに制限しました（全件は書き出しに含まれます）。
- CI の `actions/checkout` と `actions/setup-python` を v7 系に更新。

### 追加

- `docs/SECURITY_REVIEW.md`: 検証範囲、攻撃入力による実測結果、生データの残留箇所、**確かめていないこと**。
- テスト 158件 → 176件。識別子の成りすまし、証跡の追い出し、出所の偽装、非有限数、制御文字、敵対的なログ入力、LLMの異常応答、`/api/state` の要約化に対する回帰テスト。

## v0.2.0 — 2026-09-12

検知ロジックは変えていません。追加したのは、**実際のログを読めるようにすること**と、**正常な業務でどれだけ誤検知するかを測れるようにすること**です。設計書で「次の最優先」としていた項目にあたります。

### 追加

- **読み取り専用のログ取り込み**（`aisecure/connectors/`、`aisecure import`）
  CSV / JSON Lines を、検証済みマッピングプロファイル経由でスナップショットへ変換します。プロファイルは許可リストにある項目しかマッピングできないため、ファイル本文や資格情報を取り込む設定は書けません。読めない値は `null`（不明）になり、`false` で代用しません。タイムゾーンのない時刻は、明示がなければ推測せずその行を読み飛ばします。組み込みプロファイル3種、`aisecure profiles` で一覧。
- **取り込み品質レポート**（`--quality`）
  ファイルごとの行数・SHA-256・読み飛ばし理由・不明値の件数、台帳にない資産、時間幅、警告。「取り込めたか」ではなく「何が分からなかったか」を出します。
- **出所の記録**（スナップショットの `provenance`）
  取り込み元のファイル名（パスなし）とSHA-256、行数を保持し、画面と監査記録に表示します。
- **正常業務の合成トラフィック生成**（`aisecure/baseline.py`、`aisecure baseline`）
  夜間バックアップ、分析担当者の一括参照、移行作業、承認済みの保守接続を含みます。決定論的で、同じ引数なら同じバイト列になります。`--attack` でラベル付き事案を1件混ぜます。
- **誤検知評価と閾値スイープ**（`aisecure/evaluate.py`、`aisecure evaluate`）
  根拠イベントIDと事案ラベルの一致で検知/誤検知を判定し、ルール別・1日あたりの件数まで分解します。`--sweep` で大量参照の閾値と時間窓を探索し、`--save-rules` で推奨値を書き出します。
- **検知閾値の外部化**（`aisecure/rules.py`、`--rules`）
  7項目を設定ファイルで変更できます。未定義項目・範囲外の値は丸めずに拒否します。設定はハッシュ化され、起動時に `rules.configured` として監査チェーンへ記録され、各検知結果にも `rule_config_digest` が付きます。
- **AS-002 の条件切り替え**（`identity_conditions: any | both`）
  「非管理端末 **または** 未承認」と「**かつ**」を選べます。合成データでは `both` で誤検知8件が0件になり、事案は引き続き検知できました。
- **画面: 検知設定ビュー**
  現在の閾値、設定ハッシュ、測定手順、取り込み元ファイルの一覧を表示します。

### 変更

- `bytes_read` に `null`（不明）を許可。多くのファイルサーバー監査ログはサイズを記録しないため、`0` で代用しません。
- `patch_state: pending` で `vulnerability` を省略できるようにし、AS-001 が「脆弱性参照が登録されていません」として検知するようにしました。実際の台帳には「未適用」だけが入っていることがあるためです。
- 資産の `kind` に `unknown` を追加。ログにだけ現れた資産（シャドー資産）に使い、AS-005 で表面化させます。
- CLI（`import` / `analyze`）の上限を資産20,000件・イベント2,000,000件に拡大。HTTP境界の上限（2 MiB / 200資産 / 5,000イベント）は変更していません。
- 取り込み結果のスナップショット時刻は、イベントと資産台帳の新しい方に合わせます。

### 測定結果

合成データ4シナリオ（40ユーザー×5日、合計18.84日・85,509イベント）、既定の閾値:

| ルール | 件数 | 検知 | 誤検知 |
|---|---|---|---|
| AS-002 特権ログインの条件 | 10 | 1 | 9 |
| AS-003 短時間の大量ファイル参照 | 103 | 1 | 102 |
| AS-004 相関 | 1 | 1 | 0 |

閾値を上げて AS-003 の誤検知を0にすると、事案も検知できなくなります。詳細は [docs/TUNING.md](docs/TUNING.md)。**これは合成データの数値であり、実環境の誤検知率ではありません。**

### テスト

80件 → 158件。取り込み境界（許可外の列が通らないこと、不明が安全に変わらないこと、時刻を推測しないこと、シンボリックリンク・上限の拒否、再送の重複排除、パスが仮名化されて保存されること）、閾値設定の検証と監査記録、評価の数え方、CLI経路を追加しました。

### 未実装（v0.2でも変わらず）

実ログの自動収集（常時接続）、KEV・ベンダー情報の自動照合、SSO・RBAC・複数テナント、独立した監査保管、実機への対応操作、保管時暗号化と鍵管理、本番用HTTP層。

## v0.1.0 — 2026-09-11

初版。公開機器・特権ログイン・大量ファイル参照の相関検知、根拠表示、対応計画と人による承認、シミュレーション専用の実行、鍵付きハッシュチェーンによる監査記録、ループバック限定のローカルUI。標準モードは生成AIを使いません。
